# CDM Class Decisions

This file records which CDM Java classes and builder patterns each method chose,
and whether they compiled and produced correct output.
Human corrections can be added as `> CORRECTION: ...` lines.

Format:  `[iter N] method: ClassName.builder().setX() [OK|FAILED: reason]`

# CDM 6.x Type Hierarchy
# Source: FINOS CDM GitHub (branch 6.x.x), parsed from Rosetta DSL
# Built by: python scripts/build_cdm_hierarchy.py
#
# How to read:
#   type TypeName extends Parent   → Java class TypeName extends Parent
#   field: FieldType (1..1)        → required; (0..1) optional; (0..*) list
#   Java builder: TypeName.builder().setField(v).build()
#   Java list:    TypeName.builder().addField(item).build()  (for 0..*)
#
# 303 types from 8 Rosetta files

type ContractFormationInstruction  # Specifies instructions to create a fully formed contract, with optional legal agreements.
    legalAgreement: LegalAgreement (list)

type Instruction  # Instruction to a function that will be used to perform a business event
    primitiveInstruction: PrimitiveInstruction (optional)
    before: TradeState (optional)

type PrimitiveInstruction  # A Primitive Instruction describes the inputs required to pass into the corresponding PrimitiveEvent function.
    contractFormation: ContractFormationInstruction (optional)
    execution: ExecutionInstruction (optional)
    exercise: ExerciseInstruction (optional)
    partyChange: PartyChangeInstruction (optional)
    quantityChange: QuantityChangeInstruction (optional)
    reset: ResetInstruction (optional)
    split: SplitInstruction (optional)
    termsChange: TermsChangeInstruction (optional)
    transfer: TransferInstruction (optional)
    indexTransition: IndexTransitionInstruction (optional)
    stockSplit: StockSplitInstruction (optional)
    observation: ObservationInstruction (optional)
    valuation: ValuationInstruction (optional)

type BusinessEvent extends EventInstruction  # A business event represents a life cycle event of a trade. The combination of the state changes results in a qualifiable
    eventQualifier: string (optional)
    after: TradeState (list)

type CounterpartyPositionBusinessEvent  # A business event represents a life cycle event of a position. The combination of the state changes results in a qualifia
    intent: PositionEventIntentEnum (required)
    corporateActionIntent: CorporateActionTypeEnum (optional)
    eventDate: date (optional)
    effectiveDate: date (optional)
    packageInformation: IdentifiedList (optional)
    after: CounterpartyPositionState (list)

type ObservationInstruction  # Specifies inputs needed to process an observation.
    observationEvent: ObservationEvent (required)

type ValuationInstruction  # Specifies inputs needed to process a valuation.
    valuation: Valuation (list)
    replace: boolean (required)

type ExecutionInstruction  # Specifies instructions for execution of a transaction, consisting of a product, price, quantity, parties, trade identifi
    product: NonTransferableProduct (required)
    priceQuantity: PriceQuantity (list)
    counterparty: Counterparty (2..2)
    ancillaryParty: AncillaryParty (list)
    parties: Party (2..*)
    partyRoles: PartyRole (list)
    executionDetails: ExecutionDetails (required)
    tradeDate: date (required)
    tradeTime: TimeZone (optional)
    tradeIdentifier: TradeIdentifier (list)
    collateral: Collateral (optional)
    lotIdentifier: Identifier (optional)

type ExerciseInstruction  # Specifies the information required to communicate the choices made by the exercising party, in a financial product endow
    exerciseQuantity: PrimitiveInstruction (required)
    exerciseOption: OptionPayout (optional)
    exerciseDate: AdjustableOrAdjustedDate (optional)
    exerciseTime: BusinessCenterTime (optional)
    replacementTradeIdentifier: TradeIdentifier (list)

type ResetInstruction  # Defines the information needed to create a Reset Business Event.
    payout: Payout (list)
    rateRecordDate: date (optional)
    resetDate: date (required)

type CalculateTransferInstruction  # Defines the tradeState or payout on which to create a Transfer along with all necessary resets.
    tradeState: TradeState (required)
    payout: Payout (required)
    resets: Reset (list)
    payerReceiver: PayerReceiver (optional)
    quantity: Quantity (optional)
    date: date (optional)

type TransferInstruction  # Defines the payout on which to create a Transfer along with all necessary resets.
    transferState: TransferState (list)

type QuantityChangeInstruction  # Instructions required to create a Quantity Change Primitive Event, which can be either an increase, a decrease or a repl
    change: PriceQuantity (list)
    direction: QuantityChangeDirectionEnum (required)
    lotIdentifier: Identifier (list)

type IndexTransitionInstruction  # Defines the information needed to create a Index Transition Business Event.
    priceQuantity: PriceQuantity (list)
    effectiveDate: date (required)
    cashTransfer: Transfer (optional)

type TermsChangeInstruction  # Specifies instructions for terms change consisting of the new transaction terms, and the renegotiation fee.
    product: NonTransferableProduct (optional)
    ancillaryParty: AncillaryParty (list)
    adjustment: NotionalAdjustmentEnum (optional)

type SplitInstruction  # Specifies instructions for a split, consisting of a breakdown of instructions to be applied to each branch of the split.
    breakdown: PrimitiveInstruction (list)

type PartyChangeInstruction  # Specifies instruction to change the party on a trade. This primitive instruction is used in a number of scenarios includ
    counterparty: Counterparty (required)
    ancillaryParty: AncillaryParty (optional)
    partyRole: PartyRole (optional)
    tradeId: TradeIdentifier (list)

type TradeState  # Defines the fundamental financial information that can be changed by a Primitive Event and by extension any business or 
    trade: Trade (required)
    state: State (optional)
    resetHistory: Reset (list)
    transferHistory: TransferState (list)
    observationHistory: ObservationEvent (list)
    valuationHistory: Valuation (list)

type CounterpartyPositionState  # Defines the fundamental financial information that can be changed by a Primitive Event and by extension any business or 
    counterpartyPosition: CounterpartyPosition (required)
    state: State (optional)
    observationHistory: ObservationEvent (list)
    valuationHistory: Valuation (list)

type ObservationEvent  # Specifies the necessary information to create any observation event.
    creditEvent: CreditEvent (optional)
    corporateAction: CorporateAction (optional)

type Reset  # Defines the reset value or fixing value produced in cashflow calculations, during the life-cycle of a financial instrume
    resetValue: Price (required)
    resetDate: date (required)
    rateRecordDate: date (optional)
    observations: Observation (list)
    averagingMethodology: AveragingCalculation (optional)

type State  # Defines the state of a trade at a point in the Trade's life cycle. Trades have many state dimensions, all of which are r
    closedState: ClosedState (optional)
    positionState: PositionStatusEnum (optional)

type TransferState  # Defines the fundamental financial information associated with a Transfer event. Each TransferState specifies where a Tra
    transfer: Transfer (required)
    transferStatus: TransferStatusEnum (optional)

type Transfer extends AssetFlowBase  # Defines the movement of an Asset (eg cash, securities or commodities) between two parties on a date.
    identifier: Identifier (list)
    payerReceiver: PartyReferencePayerReceiver (required)
    settlementOrigin: Payout (optional)
    resetOrigin: Reset (optional)
    transferExpression: TransferExpression (required)

type TransferExpression  # Specifies a transfer expression (cash price, performance amount, scheduled payment amount, etc.) to define the nature of
    priceTransfer: FeeTypeEnum (optional)
    scheduledTransfer: ScheduledTransfer (optional)

type ScheduledTransfer
    transferType: ScheduledTransferEnum (required)
    corporateActionTransferType: CorporateActionTypeEnum (optional)

type TradeIdentifier extends Identifier  # Defines a trade identifier as a special case of the generic identifier type, that also includes the trade identifier cla
    identifierType: TradeIdentifierTypeEnum (optional)

type PositionIdentifier extends Identifier  # Defines a position identifier as a special case of the generic identifier type, that also includes the position identifi
    identifierType: TradeIdentifierTypeEnum (optional)

type Valuation  # Defines the value of an investment, asset, or security
    amount: Money (required)
    timestamp: zonedDateTime (required)
    method: ValuationTypeEnum (optional)
    source: ValuationSourceEnum (optional)
    delta: number (optional)
    valuationTiming: PriceTimingEnum (optional)
    priceComponent: Price (optional)

type Trade extends TradableProduct  # Defines the output of a financial transaction between parties - a Business Event. A Trade impacts the financial position
    tradeIdentifier: TradeIdentifier (list)
    tradeDate: date (required)
    tradeTime: TimeZone (optional)
    party: Party (list)
    partyRole: PartyRole (list)
    executionDetails: ExecutionDetails (optional)
    contractDetails: ContractDetails (optional)
    clearedDate: date (optional)
    collateral: Collateral (optional)
    account: Account (list)

type ExecutionDetails  # Defines specific attributes that relate to trade executions.
    executionType: ExecutionTypeEnum (required)
    executionVenue: LegalEntity (optional)
    packageReference: IdentifiedList (optional)

type ContractDetails  # Defines specific attributes that relate to contractual details of trades.
    documentation: LegalAgreement (list)
    governingLaw: GoverningLawEnum (optional)

type Lineage  # A class to provide lineage information across lifecycle events through a pointer or set of pointers into the event(s), c
    tradeReference: Trade (list)
    eventReference: WorkflowStep (list)
    portfolioStateReference: PortfolioState (list)

type StockSplitInstruction  # Data required to perform a stock split business event.
    adjustmentRatio: number (required)
    effectiveDate: date (required)

type CreditEvent  # Specifies the relevant data regarding a credit event.
    creditEventType: CreditEventTypeEnum (required)
    eventDeterminationDate: date (required)
    auctionDate: date (optional)
    finalPrice: Price (optional)
    recoveryPercent: number (optional)
    publiclyAvailableInformation: Resource (list)
    referenceInformation: ReferenceInformation (required)

type CorporateAction  # Specifies the relevant data regarding a corporate action.
    corporateActionType: CorporateActionTypeEnum (required)
    exDate: date (required)
    payDate: date (required)
    underlier: Underlier (required)

type ClearingInstruction  # All information required to perform the clear life cycle event; the clearing party (CCP), the two parties facing each ot
    alphaContract: TradeState (required)
    clearingParty: Party (required)
    party1: Party (required)
    party2: Party (required)
    clearerParty1: Party (optional)
    clearerParty2: Party (optional)
    isOpenOffer: boolean (optional)

type ExerciseEvent  # A data defining:  the adjusted dates associated with a particular exercise event.
    adjustedExerciseDate: date (required)
    adjustedRelevantSwapEffectiveDate: date (required)
    adjustedCashSettlementValuationDate: date (optional)
    adjustedCashSettlementPaymentDate: date (optional)
    adjustedExerciseFeePaymentDate: date (optional)

type ReturnInstruction  # Specifies the information required to create the return of a Security Finance Transaction.
    quantity: Quantity (list)

type SecurityLendingInvoice  # Specifies the information required for inclusion in a securities lending billing invoice.
    sendingParty: Party (required)
    receivingParty: Party (required)
    billingStartDate: date (required)
    billingEndDate: date (required)
    billingRecord: BillingRecord (list)
    billingSummary: BillingSummary (list)

type BillingInstruction  # Specifies the instructions for creation of a Security Lending billing invoice.
    sendingParty: Party (required)
    receivingParty: Party (required)
    billingStartDate: date (required)
    billingEndDate: date (required)
    billingRecordInstruction: BillingRecordInstruction (list)
    billingSummary: BillingSummaryInstruction (list)

type BillingRecordInstruction  # Specifies the instructions for creation of a billing record.
    tradeState: TradeState (required)
    observation: Observation (list)
    recordStartDate: date (required)
    recordEndDate: date (required)
    settlementDate: date (required)

type BillingSummaryInstruction  # Specifies the instructions for creation of a billing summary.
    summaryAmountType: RecordAmountTypeEnum (required)

type BillingRecord  # Specifies individual records within a billing invoice.
    tradeState: TradeState (required)
    recordTransfer: Transfer (required)
    recordStartDate: date (required)
    recordEndDate: date (required)
    minimumFee: Money (optional)

type BillingSummary  # Specifies individual summaries within a billing invoice.
    summaryTransfer: Transfer (optional)
    summaryAmountType: RecordAmountTypeEnum (required)

type TradePricingReport  # The attributes that are specific for consensus based pricing reporting.
    trade: Trade (required)
    pricingTime: TimeZone (required)
    discountingIndex: FloatingRateIndexEnum (optional)

type MarginCallBase  # Represents common attributes required for Issuance and Response to a Margin Call action as a result of a demand for deli
    instructionType: MarginCallInstructionType (required)
    party: Party (list)
    partyRole: PartyRole (list)
    clearingBroker: Party (optional)
    callIdentifier: Identifier (optional)
    callAgreementType: AgreementName (required)
    agreementMinimumTransferAmount: Money (optional)
    agreementThreshold: Money (optional)
    agreementRounding: Money (optional)
    regMarginType: RegMarginTypeEnum (required)
    regIMRole: RegIMRoleEnum (optional)
    baseCurrencyExposure: MarginCallExposure (optional)
    collateralPortfolio: CollateralPortfolio (optional)
    independentAmountBalance: CollateralBalance (optional)

type Exposure  # Represents the current mark to market value or IM calculation value of the trade portfolio as recorded by the principle 
    tradePortfolio: PortfolioState (required)
    aggregateValue: Money (required)
    calculationDateTime: zonedDateTime (optional)
    valuationDateTime: zonedDateTime (required)

type MarginCallExposure extends MarginCallBase  # Represents attributes required for mark to market value or IM calculation value of the trade portfolio as recorded by th
    overallExposure: Exposure (required)
    simmIMExposure: Exposure (optional)
    scheduleGridIMExposure: Exposure (optional)

type CollateralBalance  # Represents common attributes to define a collateral balance recorded by the principal as held or posted.
    collateralBalanceStatus: CollateralStatusEnum (optional)
    haircutIndicator: HaircutIndicatorEnum (optional)
    amountBaseCurrency: Money (required)
    payerReceiver: PartyReferencePayerReceiver (required)

type CollateralPortfolio  # Represents common attributes to define the details of collateral assets, to be used in margin call messaging and contrib
    portfolioIdentifier: Identifier (optional)
    collateralPosition: CollateralPosition (list)
    collateralBalance: CollateralBalance (list)
    legalAgreement: LegalAgreement (optional)

type CollateralPosition extends Position  # Specifies the individual components of collateral positions.
    treatment: CollateralTreatment (optional)
    collateralPositionStatus: CollateralStatusEnum (optional)

type MarginCallIssuance extends MarginCallBase  # Represents common attributes required for a Margin Call Issuance under a legal agreement such as a credit support docume
    callAmountInBaseCurrency: Money (required)
    recallNonCashCollateralDescription: EligibleCollateralCriteria (list)

type MarginCallInstructionType  # Represents enumeration values to specify the call notification type, direction, specific action type.
    callType: CallTypeEnum (required)
    visibilityIndicator: boolean (optional)

type MarginCallResponseAction  # Specifies the margin call action details, including collateral to be moved and its direction.
    collateralPositionComponent: CollateralPosition (list)
    marginCallAction: MarginCallActionEnum (required)

type MarginCallResponse extends MarginCallBase  # Represents common attributes required for a Margin Call Response under a legal agreement such as a credit support docume
    marginCallResponseAction: MarginCallResponseAction (list)
    marginResponseType: MarginCallResponseTypeEnum (required)
    agreedAmountBaseCurrency: Money (required)

type ClosedState  # A class to qualify the closed state of an execution or a contract through the combination or a state (e.g. terminated, n
    state: ClosedStateEnum (required)
    activityDate: date (required)
    effectiveDate: date (optional)
    lastPaymentDate: date (optional)

type TransferableProduct extends Asset  # A TransferableProduct is a type of financial product which can be held or transferred, represented as an Asset with the 
    economicTerms: EconomicTerms (required)

type EconomicTerms  # This class represents the full set of price-forming features associated with a contractual product: the payout component
    effectiveDate: AdjustableOrRelativeDate (optional)
    terminationDate: AdjustableOrRelativeDate (optional)
    dateAdjustments: BusinessDayAdjustments (optional)
    payout: Payout (list)
    terminationProvision: TerminationProvision (optional)
    calculationAgent: CalculationAgent (optional)
    nonStandardisedTerms: boolean (optional)
    collateral: Collateral (optional)

type OptionPayout extends PayoutBase  # The option payout specification terms. The associated globalKey denotes the ability to associate a hash value to the res
    buyerSeller: BuyerSeller (required)
    feature: OptionFeature (optional)
    observationTerms: ObservationTerms (optional)
    schedule: CalculationSchedule (optional)
    delivery: AssetDeliveryInformation (optional)
    underlier: Underlier (required)
    optionType: OptionTypeEnum (optional)
    exerciseTerms: ExerciseTerms (required)
    strike: OptionStrike (optional)

type ReturnTerms  # Specifies the type of return of a performance payout.
    priceReturnTerms: PriceReturnTerms (optional)
    dividendReturnTerms: DividendReturnTerms (optional)
    varianceReturnTerms: VarianceReturnTerms (optional)
    volatilityReturnTerms: VolatilityReturnTerms (optional)
    correlationReturnTerms: CorrelationReturnTerms (optional)

type PerformancePayout extends PayoutBase  # Contains the necessary specifications for all performance payouts, encompassing equity return, dividend, variance, volat
    observationTerms: ObservationTerms (optional)
    valuationDates: ValuationDates (required)
    paymentDates: PaymentDates (required)
    underlier: Underlier (optional)
    fxFeature: FxFeature (list)
    returnTerms: ReturnTerms (optional)
    portfolioReturnTerms: PortfolioReturnTerms (list)
    initialValuationPrice: PriceSchedule (list)
    interimValuationPrice: PriceSchedule (list)
    finalValuationPrice: PriceSchedule (list)

type PortfolioReturnTerms extends ReturnTerms  # Specifies an individual type of return of a Performance Payout, when such individual return is part of an aggregation of
    payerReceiver: PayerReceiver (required)
    underlier: Observable (required)
    quantity: NonNegativeQuantitySchedule (optional)
    initialValuationPrice: PriceSchedule (list)
    interimValuationPrice: PriceSchedule (list)
    finalValuationPrice: PriceSchedule (list)

type NonTransferableProduct  # A data type to specify the financial product's economic terms, alongside the product identification and product taxonomy
    identifier: ProductIdentifier (list)
    taxonomy: ProductTaxonomy (list)
    economicTerms: EconomicTerms (required)

type TradeLot  # Specifies the price and quantity of a trade lot, where the same product could be traded multiple times with the same cou
    lotIdentifier: Identifier (list)
    priceQuantity: PriceQuantity (list)

type TradableProduct  # Definition of a product as ready to be traded, i.e. included in an execution or contract, by associating a specific pric
    product: NonTransferableProduct (required)
    tradeLot: TradeLot (list)
    counterparty: Counterparty (2..2)
    ancillaryParty: AncillaryParty (list)
    adjustment: NotionalAdjustmentEnum (optional)

type ConstituentWeight  # A class describing the weight of each of the underlier constituent within the basket, either in absolute or relative ter
    openUnits: number (optional)
    basketPercentage: number (optional)

type SettlementPayout extends PayoutBase  # Represents a forward settling payout. The underlier attribute captures the underlying payout, which is settled according
    underlier: Underlier (required)
    deliveryTerm: string (optional)
    delivery: AssetDeliveryInformation (optional)
    schedule: CalculationSchedule (optional)

type CalculationSchedule  # A class that allows the full representation of a payout by defining a set of schedule periods. It supports standard sche
    schedulePeriod: SchedulePeriod (list)

type SchedulePeriod  # A class that defines the period of a schedule. The period contains a set of start and end dates, quantities, fixing, and
    calculationPeriod: DateRange (required)
    paymentDate: date (required)
    fixingPeriod: DateRange (required)
    deliveryPeriod: CalculationScheduleDeliveryPeriods (optional)

type FixedPricePayout extends PayoutBase  # Represents a fixed price payout. There is no underlier associated with this payout type and is based on fixed pricing pe
    paymentDates: PaymentDates (required)
    fixedPrice: FixedPrice (required)
    schedule: CalculationSchedule (optional)

type CancelableProvision extends BuyerSeller  # A data defining:  the right of a party to cancel a swap transaction on the specified exercise dates. The provision is fo
    exerciseNotice: ExerciseNotice (optional)
    followUpConfirmation: boolean (required)
    cancelableProvisionAdjustedDates: CancelableProvisionAdjustedDates (optional)
    finalCalculationPeriodDateAdjustment: FinalCalculationPeriodDateAdjustment (list)
    initialFee: Transfer (optional)
    callingParty: CallingPartyEnum (optional)
    earliestDate: AdjustableOrRelativeDate (optional)
    expirationDate: AdjustableOrRelativeDate (optional)
    effectiveDate: AdjustableOrRelativeDates (optional)
    effectivePeriod: Period (optional)
    earliestCancellationTime: BusinessCenterTime (optional)
    latestCancelationTime: BusinessCenterTime (optional)
    exerciseTerms: ExerciseTerms (required)

type CancelableProvisionAdjustedDates  # A data to:  define the adjusted dates for a cancelable provision on a swap transaction.
    cancellationEvent: CancellationEvent (list)

type CancellationEvent  # The adjusted dates for a specific cancellation date, including the adjusted exercise date and adjusted termination date.
    adjustedExerciseDate: date (required)
    adjustedEarlyTerminationDate: date (required)

type EarlyTerminationEvent  # A data to:  define the adjusted dates associated with an early termination provision.
    adjustedExerciseDate: date (required)
    adjustedEarlyTerminationDate: date (required)
    adjustedCashSettlementValuationDate: date (required)
    adjustedCashSettlementPaymentDate: date (required)
    adjustedExerciseFeePaymentDate: date (optional)

type EarlyTerminationProvision  # A data defining:  an early termination provision for a swap. This early termination is at fair value, i.e. on terminatio
    mandatoryEarlyTermination: MandatoryEarlyTermination (optional)
    mandatoryEarlyTerminationDateTenor: Period (optional)
    optionalEarlyTermination: OptionalEarlyTermination (optional)
    optionalEarlyTerminationParameters: ExercisePeriod (optional)

type ExercisePeriod  # This defines the time interval to the start of the exercise period, i.e. the earliest exercise date, and the frequency o
    earliestExerciseDateTenor: Period (required)
    exerciseFrequency: Period (optional)

type ExtendibleProvision extends BuyerSeller  # A data defining:  an option to extend an existing swap transaction on the specified exercise dates for a term ending on 
    exerciseNotice: ExerciseNotice (optional)
    followUpConfirmation: boolean (optional)
    extendibleProvisionAdjustedDates: ExtendibleProvisionAdjustedDates (optional)
    callingParty: CallingPartyEnum (optional)
    singlePartyOption: PartyRole (optional)
    noticeDeadlinePeriod: RelativeDateOffset (optional)
    noticeDeadlineDateTime: zonedDateTime (optional)
    extensionTerm: RelativeDateOffset (optional)
    extensionPeriod: AdjustableRelativeOrPeriodicDates (optional)
    exerciseTerms: ExerciseTerms (required)

type ExtendibleProvisionAdjustedDates  # A data defining:  the adjusted dates associated with a provision to extend a swap.
    extensionEvent: ExtensionEvent (list)

type ExtensionEvent  # A data to:  define the adjusted dates associated with an individual extension event.
    adjustedExerciseDate: date (required)
    adjustedExtendedTerminationDate: date (required)

type MandatoryEarlyTermination  # A data to:  define an early termination provision for which exercise is mandatory.
    mandatoryEarlyTerminationDate: AdjustableDate (required)
    calculationAgent: CalculationAgent (required)
    cashSettlement: SettlementTerms (required)
    mandatoryEarlyTerminationAdjustedDates: MandatoryEarlyTerminationAdjustedDates (optional)

type MandatoryEarlyTerminationAdjustedDates  # A data defining:  the adjusted dates associated with a mandatory early termination provision.
    adjustedEarlyTerminationDate: date (required)
    adjustedCashSettlementValuationDate: date (required)
    adjustedCashSettlementPaymentDate: date (required)

type OptionalEarlyTermination  # A data defining:  an early termination provision where either or both parties have the right to exercise.
    singlePartyOption: BuyerSeller (optional)
    mutualEarlyTermination: boolean (optional)
    exerciseNotice: ExerciseNotice (list)
    followUpConfirmation: boolean (optional)
    calculationAgent: CalculationAgent (optional)
    cashSettlement: SettlementTerms (optional)
    optionalEarlyTerminationAdjustedDates: OptionalEarlyTerminationAdjustedDates (optional)
    exerciseTerms: ExerciseTerms (required)

type OptionalEarlyTerminationAdjustedDates  # A data defining:  the adjusted dates associated with an optional early termination provision.
    earlyTerminationEvent: EarlyTerminationEvent (list)

type Asian  # As per ISDA 2002 Definitions.
    averagingInOut: AveragingInOutEnum (required)
    strikeFactor: number (optional)
    averagingPeriodIn: AveragingPeriod (optional)
    averagingPeriodOut: AveragingPeriod (optional)

type AveragingCalculation  # Defines parameters for use in cases when a valuation or other term is based on an average of market observations.
    averagingMethod: AveragingCalculationMethod (required)
    precision: Rounding (required)

type AveragingStrikeFeature  # Defines the terms required to calculate the average observations associated with an averaging strike.
    averagingCalculation: AveragingCalculation (required)
    observationTerms: ObservationTerms (required)

type Barrier  # As per ISDA 2002 Definitions.
    barrierCap: TriggerEvent (list)
    barrierFloor: TriggerEvent (list)

type CalendarSpread  # A type for defining a calendar spread feature.
    expirationDateTwo: AdjustableOrRelativeDate (required)

type Composite  # Specifies the conditions to be applied for converting into a reference currency when the actual currency rate is not det
    determinationMethod: DeterminationMethodEnum (optional)
    relativeDate: RelativeDateOffset (optional)
    fxSpotRateSource: FxSpotRateSource (optional)
    fixingTime: BusinessCenterTime (optional)

type FxFeature  # A type for defining FX Features.
    referenceCurrency: string (required)
    composite: Composite (optional)
    quanto: Quanto (optional)
    crossCurrency: Composite (optional)

type Knock  # Knock In means option to exercise comes into existence. Knock Out means option to exercise goes out of existence.
    knockIn: TriggerEvent (list)
    knockOut: TriggerEvent (list)

type OptionFeature  # Defines additional optional features that can be included in an option contract.
    fxFeature: FxFeature (list)
    strategyFeature: StrategyFeature (optional)
    averagingFeature: AveragingCalculation (optional)
    barrier: Barrier (optional)
    knock: Knock (optional)
    passThrough: PassThrough (optional)

type TerminationProvision  # A class for defining option provisions.
    cancelableProvision: CancelableProvision (optional)
    earlyTerminationProvision: EarlyTerminationProvision (optional)
    evergreenProvision: EvergreenProvision (optional)
    extendibleProvision: ExtendibleProvision (optional)

type OptionStrike  # Defines the strike price of an option.
    strikePrice: Price (optional)
    strikeReference: FixedRateSpecification (optional)
    referenceSwapCurve: ReferenceSwapCurve (optional)
    averagingStrikeFeature: AveragingStrikeFeature (optional)

type PassThrough  # Type which contains pass through payments.
    passThroughItem: PassThroughItem (list)

type PassThroughItem  # Class to represent a single pass through payment.
    payerReceiver: PayerReceiver (required)
    passThroughPercentage: number (required)

type Quanto  # Determines the currency rate that the seller of the equity amounts will apply at each valuation date for converting the 
    fxRate: FxRate (list)
    fxSpotRateSource: FxSpotRateSource (optional)
    fixingTime: BusinessCenterTime (optional)

type StrategyFeature  # A class for defining option strategy features.
    strikeSpread: StrikeSpread (optional)
    calendarSpread: CalendarSpread (optional)

type StrikeSpread  # A class for defining a strike spread feature.
    upperStrike: OptionStrike (required)
    upperStrikeNumberOfOptions: number (required)

type ExerciseTerms  # A class defining the exercise period for an option together with any rules governing the notional amount of the underlyi
    style: OptionExerciseStyleEnum (optional)
    commencementDate: AdjustableOrRelativeDate (optional)
    exerciseDates: AdjustableOrRelativeDates (optional)
    expirationDate: AdjustableOrRelativeDate (list)
    relevantUnderlyingDate: AdjustableOrRelativeDates (optional)
    earliestExerciseTime: BusinessCenterTime (optional)
    latestExerciseTime: BusinessCenterTime (optional)
    expirationTime: BusinessCenterTime (optional)
    expirationTimeType: ExpirationTimeTypeEnum (required)
    multipleExercise: MultipleExercise (optional)
    exerciseFeeSchedule: ExerciseFeeSchedule (optional)
    exerciseProcedure: ExerciseProcedure (optional)
    exerciseFee: ExerciseFee (optional)
    partialExercise: PartialExercise (optional)

type AutomaticExercise  # A type to define automatic exercise of a swaption. With automatic exercise the option is deemed to have exercised if it 
    thresholdRate: number (optional)
    isApplicable: boolean (optional)

type ExerciseFee extends PayerReceiver  # A class defining the fee payable on exercise of an option. This fee may be defined as an amount or a percentage of the n
    notionalReference: Money (required)
    feeAmount: number (optional)
    feeRate: number (optional)
    feePaymentDate: RelativeDateOffset (required)

type ExerciseFeeSchedule extends PayerReceiver  # A class to define a fee or schedule of fees to be payable on the exercise of an option. This fee may be defined as an am
    notionalReference: Money (required)
    feeAmountSchedule: AmountSchedule (optional)
    feeRateSchedule: Schedule (optional)
    feePaymentDate: RelativeDateOffset (required)

type ExerciseNotice  # Defines to whom and where notice of execution should be given. The exerciseNoticeGiver refers to one or both of the prin
    exerciseNoticeGiver: ExerciseNoticeGiverEnum (required)
    exerciseNoticeReceiver: AncillaryRoleEnum (optional)
    businessCenter: BusinessCenterEnum (required)

type ExerciseProcedure  # A class describing how notice of exercise should be given. This can be either manual or automatic.
    manualExercise: ManualExercise (optional)
    automaticExercise: AutomaticExercise (optional)
    followUpConfirmation: boolean (required)
    limitedRightToConfirm: boolean (optional)
    splitTicket: boolean (optional)

type ManualExercise  # A class defining manual exercise, i.e. that the option buyer counterparty must give notice to the option seller of exerc
    exerciseNotice: ExerciseNotice (optional)
    fallbackExercise: boolean (optional)

type MultipleExercise extends PartialExercise  # A class defining multiple exercises. As defined in the 2000 ISDA Definitions, Section 12.4. Multiple Exercise, the buyer
    maximumNotionalAmount: number (optional)
    maximumNumberOfOptions: int (optional)

type PartialExercise  # A class defining partial exercise. As defined in the 2000 ISDA Definitions, Section 12.3 Partial Exercise, the buyer of 
    notionaReference: Money (required)
    integralMultipleAmount: number (optional)
    minimumNotionalAmount: number (optional)
    minimumNumberOfOptions: int (optional)

type Strike  # A class describing a single cap or floor rate.
    strikeRate: number (required)
    buyer: PayerReceiverEnum (optional)
    seller: PayerReceiverEnum (optional)

type StrikeSchedule extends RateSchedule  # A class describing a schedule of cap or floor rates.
    buyer: PayerReceiverEnum (optional)
    seller: PayerReceiverEnum (optional)

type AssetPayout extends PayoutBase  # Security finance payout specification in case the product payout involves some form of security collateral, as in a secu
    assetLeg: AssetLeg (list)
    underlier: Asset (required)
    minimumFee: Money (optional)
    dividendTerms: DividendTerms (optional)
    tradeType: AssetPayoutTradeTypeEnum (optional)

type DividendTerms  # Information related to dividends and payments.
    manufacturedIncomeRequirement: DividendPayoutRatio (required)
    dividendEntitlement: DividendEntitlementEnum (optional)
    minimumBillingAmount: Money (optional)

type AssetLeg  # Defines each asset movement of an asset payout.
    settlementDate: AdjustableOrRelativeDate (required)
    deliveryMethod: DeliveryMethodEnum (required)

type EvergreenProvision  # Specifies a transaction which automatically extends for a specified timeframe until the exercise of an embedded option.
    singlePartyOption: PartyRole (optional)
    noticePeriod: RelativeDateOffset (required)
    noticeDeadlinePeriod: RelativeDateOffset (optional)
    noticeDeadlineDateTime: zonedDateTime (optional)
    extensionFrequency: AdjustableRelativeOrPeriodicDates (required)
    finalPeriodFeeAdjustment: Price (optional)

type CommodityPayout extends PayoutBase  # Payout based on the averaged price of a referenced underlier. (e.g. Commodities). Can represent both average (average of
    averagingFeature: AveragingCalculation (optional)
    commodityPriceReturnTerms: CommodityPriceReturnTerms (optional)
    pricingDates: PricingDates (required)
    schedule: CalculationSchedule (optional)
    calculationPeriodDates: CalculationPeriodDates (optional)
    paymentDates: PaymentDates (required)
    underlier: Underlier (required)
    fxFeature: FxFeature (optional)
    delivery: AssetDeliveryInformation (optional)

type AssetDeliveryInformation  # Contains the information relative to the delivery of the asset.
    periods: AssetDeliveryPeriods (optional)
    location: LocationIdentifier (list)
    deliveryCapacity: Quantity (optional)
    commodityGrade: ProductGradeEnum (list)

type AssetDeliveryPeriods  # Defines the periods of delivery, including the delivery profile.
    profile: AssetDeliveryProfile (list)
    startDate: date (optional)
    endDate: date (optional)

type CalculationScheduleDeliveryPeriods extends AssetDeliveryPeriods  # Period and time profile over which the delivery takes place.
    deliveryCapacity: Quantity (optional)
    priceTimeIntervalQuantity: Price (optional)

type AssetDeliveryProfile  # Defines the delivery profile of the asset, including the load type and the delivery intervals.
    loadType: LoadTypeEnum (optional)
    block: AssetDeliveryProfileBlock (list)
    bankHolidaysTreatment: BankHolidayTreatmentEnum (optional)

type AssetDeliveryProfileBlock  # Defines a delivery profile block, including start and end time, days of the week, duration, delivery capacity and price 
    startTime: time (optional)
    endTime: time (optional)
    dayOfWeek: DayOfWeekEnum (0..7)
    deliveryCapacity: Quantity (optional)
    priceTimeIntervalQuantity: Price (optional)

type CreditDefaultPayout extends PayoutBase  # The credit default payout specification provides the details necessary for determining when a credit payout will be trig
    generalTerms: GeneralTerms (required)
    protectionTerms: ProtectionTerms (list)
    transactedPrice: TransactedPrice (optional)

type GeneralTerms  # A class specifying a set of non-monetary terms for the Credit Derivative Transaction, including the buyer and seller and
    referenceInformation: ReferenceInformation (optional)
    indexReferenceInformation: CreditIndex (optional)
    basketReferenceInformation: BasketReferenceInformation (optional)
    additionalTerm: string (list)
    substitution: boolean (optional)
    modifiedEquityDelivery: boolean (optional)

type InterestRatePayout extends PayoutBase  # A class to specify all of the terms necessary to define and calculate a cash flow based on a fixed, a floating or an inf
    rateSpecification: RateSpecification (optional)
    dayCountFraction: DayCountFractionEnum (optional)
    calculationPeriodDates: CalculationPeriodDates (optional)
    paymentDates: PaymentDates (optional)
    paymentDate: AdjustableDate (optional)
    paymentDelay: boolean (optional)
    resetDates: ResetDates (optional)
    discountingMethod: DiscountingMethod (optional)
    compoundingMethod: CompoundingMethodEnum (optional)
    cashflowRepresentation: CashflowRepresentation (optional)
    stubPeriod: StubPeriod (optional)
    bondReference: BondReference (optional)
    fixedAmount: calculation (optional)
    floatingAmount: calculation (optional)
    spreadCalculationMethod: SpreadCalculationMethodEnum (optional)

type DividendPayoutRatio  # A class describing the dividend payout ratio associated with an equity underlier. In certain cases the actual ratio is n
    totalRatio: number (required)
    cashRatio: number (optional)
    nonCashRatio: number (optional)
    basketConstituent: BasketConstituent (optional)

type AdditionalFixedPayments  # A class to specify the events that will give rise to the payment additional fixed payments.
    interestShortfallReimbursement: boolean (optional)
    principalShortfallReimbursement: boolean (optional)
    writedownReimbursement: boolean (optional)

type BasketReferenceInformation  # CDS Basket Reference Information.
    basketName: string (optional)
    basketId: string (list)
    referencePool: ReferencePool (required)
    nthToDefault: int (optional)
    mthToDefault: int (optional)
    tranche: Tranche (optional)

type FloatingAmountEvents  # A class to specify the ISDA terms relating to the floating rate payment events and the implied additional fixed payments
    failureToPayPrincipal: boolean (optional)
    interestShortfall: InterestShortFall (optional)
    writedown: boolean (optional)
    impliedWritedown: boolean (optional)
    floatingAmountProvisions: FloatingAmountProvisions (optional)
    additionalFixedPayments: AdditionalFixedPayments (optional)

type FloatingAmountProvisions
    wacCapInterestProvision: boolean (optional)
    stepUpProvision: boolean (optional)

type InterestShortFall  # A class to specify the interest shortfall floating rate payment event.
    interestShortfallCap: InterestShortfallCapEnum (required)
    compounding: boolean (required)
    rateSource: FloatingRateIndexEnum (optional)

type ProtectionTerms  # A class to specify the terms for calculating a payout to protect the buyer of the swap in the case of a qualified credit
    creditEvents: CreditEvents (optional)
    obligations: Obligations (optional)
    floatingAmountEvents: FloatingAmountEvents (optional)

type ReferenceInformation  # A class specifying the Credit Default Swap Reference Information.
    referenceEntity: LegalEntity (required)
    referenceObligation: ReferenceObligation (list)
    noReferenceObligation: boolean (optional)
    unknownReferenceObligation: boolean (optional)
    allGuarantees: boolean (optional)
    referencePrice: Price (optional)
    referencePolicy: boolean (optional)
    securedList: boolean (optional)

type ReferenceObligation  # A class to specify the reference obligation that is associated with a credit derivative instrument.
    security: Security (optional)
    loan: Loan (optional)
    primaryObligor: LegalEntity (optional)
    primaryObligorReference: LegalEntity (optional)
    guarantor: LegalEntity (optional)
    guarantorReference: string (optional)
    standardReferenceObligation: boolean (optional)

type ReferencePair
    referenceEntity: LegalEntity (required)
    referenceObligation: ReferenceObligation (optional)
    noReferenceObligation: boolean (optional)
    entityType: EntityTypeEnum (required)

type ReferencePool  # This type contains all the reference pool items to define the reference entity and reference obligation(s) in the basket
    referencePoolItem: ReferencePoolItem (list)

type ReferencePoolItem  # This type contains all the constituent weight and reference information.
    constituentWeight: ConstituentWeight (optional)
    referencePair: ReferencePair (required)
    protectionTermsReference: ProtectionTerms (optional)
    cashSettlementTermsReference: CashSettlementTerms (optional)
    physicalSettlementTermsReference: PhysicalSettlementTerms (optional)

type SettledEntityMatrix  # A class to specify the Relevant Settled Entity Matrix.
    matrixSource: SettledEntityMatrixSourceEnum (required)
    publicationDate: date (optional)

type Tranche  # The class to represent a CDS Tranche.
    attachmentPoint: number (required)
    exhaustionPoint: number (required)
    incurredRecoveryApplicable: boolean (optional)

type DividendCurrency  # A class to specify the currency in which the dividends will be denominated, i.e. either in the dividend currency or in a
    currency: string (optional)
    determinationMethod: DeterminationMethodEnum (optional)
    currencyReference: string (optional)

type DividendDateReference  # A class to specify the dividend date by reference to another date, with the ability to apply and offset. This class does
    dateReference: DividendDateReferenceEnum (required)
    paymentDateOffset: Offset (optional)

type DividendPaymentDate  # A class describing the date on which the dividend will be paid/received. This class is also used to specify the date on 
    dividendDateReference: DividendDateReference (optional)
    dividendDate: AdjustableOrRelativeDate (optional)

type DividendReturnTerms  # A class describing the conditions governing the payment of dividends to the receiver of the equity return, with the exce
    dividendPayoutRatio: DividendPayoutRatio (list)
    dividendReinvestment: boolean (optional)
    dividendEntitlement: DividendEntitlementEnum (optional)
    dividendAmountType: DividendAmountTypeEnum (optional)
    performance: calculation (optional)
    firstOrSecondPeriod: DividendPeriodEnum (optional)
    extraordinaryDividendsParty: AncillaryRoleEnum (optional)
    excessDividendAmount: DividendAmountTypeEnum (optional)
    dividendCurrency: DividendCurrency (optional)
    nonCashDividendTreatment: NonCashDividendTreatmentEnum (optional)
    dividendComposition: DividendCompositionEnum (optional)
    specialDividends: boolean (optional)
    materialDividend: boolean (optional)
    dividendPeriod: DividendPeriod (list)

type DividendPeriod  # Time bounded dividend payment periods, each with a dividend payment date per period.
    startDate: DividendPaymentDate (optional)
    endDate: DividendPaymentDate (optional)
    dateAdjustments: BusinessDayAdjustments (required)
    basketConstituent: BasketConstituent (optional)
    dividendPaymentDate: DividendPaymentDate (required)
    dividendValuationDate: AdjustableOrRelativeDate (optional)

type PriceReturnTerms
    returnType: ReturnTypeEnum (required)
    conversionFactor: number (optional)
    performance: calculation (optional)

type ReturnTermsBase  # Contains all common elements in variance, volatility and correlation return Terms.
    valuationTerms: ValuationTerms (required)
    annualizationFactor: int (optional)
    dividendApplicability: DividendApplicability (optional)
    equityUnderlierProvisions: EquityUnderlierProvisions (optional)
    sharePriceDividendAdjustment: boolean (optional)
    expectedN: int (required)
    initialLevel: number (optional)
    initialLevelSource: DeterminationMethodEnum (optional)
    meanAdjustment: boolean (optional)
    performance: calculation (optional)

type VarianceReturnTerms extends ReturnTermsBase
    varianceStrikePrice: Price (optional)
    volatilityStrikePrice: Price (optional)
    varianceCapFloor: VarianceCapFloor (optional)
    volatilityCapFloor: VolatilityCapFloor (optional)
    vegaNotionalAmount: NonNegativeQuantitySchedule (optional)
    exchangeTradedContractNearest: Observable (optional)

type VolatilityReturnTerms extends ReturnTermsBase
    volatilityStrikePrice: Price (required)
    volatilityCapFloor: VolatilityCapFloor (optional)
    exchangeTradedContractNearest: ListedDerivative (optional)

type CorrelationReturnTerms extends ReturnTermsBase
    correlationStrikePrice: Price (required)
    boundedCorrelation: NumberRange (optional)
    numberOfDataSeries: int (optional)

type ValuationTerms
    futuresPriceValuation: boolean (optional)
    optionsPriceValuation: boolean (optional)
    numberOfValuationDates: int (optional)
    dividendValuationDates: AdjustableRelativeOrPeriodicDates (optional)
    fPVFinalPriceElectionFallback: FPVFinalPriceElectionFallbackEnum (optional)
    multipleExchangeIndexAnnexFallback: boolean (optional)
    componentSecurityIndexAnnexFallback: boolean (optional)

type EquityUnderlierProvisions
    multipleExchangeIndexAnnexFallback: boolean (optional)
    componentSecurityIndexAnnexFallback: boolean (optional)
    localJurisdiction: string (optional)
    relevantJurisdiction: string (optional)

type VarianceCapFloor
    varianceCap: boolean (required)
    unadjustedVarianceCap: number (optional)
    boundedVariance: BoundedVariance (optional)

type BoundedVariance
    realisedVarianceMethod: RealisedVarianceMethodEnum (required)
    daysInRangeAdjustment: boolean (required)
    upperBarrier: number (optional)
    lowerBarrier: number (optional)

type VolatilityCapFloor  # Contains volatility-based barriers. Volatility Cap needs to be specified in accordance with the ISDA 2011 Equity Derivat
    applicable: boolean (required)
    totalVolatilityCap: number (optional)
    volatilityCapFactor: number (optional)

type BoundedCorrelation  # Describes correlation bounds, which form a cap and a floor on the realized correlation.
    minimumBoundaryPercent: number (optional)
    maximumBoundaryPercent: number (optional)

type ForeignExchange  # From FpML: A type defining either a spot or forward FX transactions.
    exchangedCurrency1: Cashflow (required)
    exchangedCurrency2: Cashflow (required)
    tenorPeriod: Period (optional)

type BondReference  # Reference to a bond underlier to represent an asset swap or Condition Precedent Bond.
    bond: Security (required)
    conditionPrecedentBond: boolean (required)
    discrepancyClause: boolean (optional)
    couponRate: FixedRateSpecification (optional)

type CashflowRepresentation  # A data defining:  the cashflow representation of a swap trade.
    cashflowsMatchParameters: boolean (required)
    paymentCalculationPeriod: PaymentCalculationPeriod (list)

type DiscountingMethod  # A data defining:  discounting information. The 2000 ISDA definitions, section 8.4. discounting (related to the calculati
    discountingType: DiscountingTypeEnum (required)
    discountRate: number (optional)
    discountRateDayCountFraction: DayCountFractionEnum (optional)

type FloatingRateDefinition  # A data defining:  parameters associated with a floating rate reset. This data forms:  part of the cashflows representati
    calculatedRate: number (optional)
    rateObservation: RateObservation (list)
    floatingRateMultiplier: number (optional)
    spread: number (optional)
    capRate: Strike (list)
    floorRate: Strike (list)

type InflationRateSpecification extends FloatingRateSpecification  # A data to:  specify the inflation rate.
    inflationLag: Offset (required)
    indexSource: string (required)
    mainPublication: string (required)
    interpolationMethod: InterpolationMethodEnum (required)
    initialIndexLevel: number (optional)
    fallbackBondApplicable: boolean (required)
    calculationMethod: InflationCalculationMethodEnum (optional)
    calculationStyle: InflationCalculationStyleEnum (optional)
    finalPrincipalExchangeCalculation: FinalPrincipalExchangeCalculationEnum (optional)

type FloatingRateBase  # A class defining a floating interest rate through the specification of the floating rate index, the tenor, the multiplie
    rateOption: InterestRateIndex (optional)
    spreadSchedule: SpreadSchedule (optional)
    capRateSchedule: StrikeSchedule (optional)
    floorRateSchedule: StrikeSchedule (optional)

type FloatingRate extends FloatingRateBase
    floatingRateMultiplierSchedule: RateSchedule (optional)
    rateTreatment: RateTreatmentEnum (optional)
    calculationParameters: FloatingRateCalculationParameters (optional)
    fallbackRate: FallbackRateParameters (optional)

type FloatingRateSpecification extends FloatingRate  # A class to specify the floating interest rate by extending the floating rate definition with a set of attributes that sp
    initialRate: Price (optional)
    finalRateRounding: Rounding (optional)
    averagingMethod: AveragingWeightingMethodEnum (optional)
    negativeInterestRateTreatment: NegativeInterestRateTreatmentEnum (optional)

type FixedRateSpecification  # Type defining the specification for a fixed rate.
    rateSchedule: RateSchedule (optional)

type FutureValueAmount  # A class defining a currency and a future value date.
    quantity: NonNegativeQuantitySchedule (optional)
    currency: string (required)
    calculationPeriodNumberOfDays: int (required)
    valueDate: date (required)

type SpreadSchedule extends RateSchedule  # Adds an optional spread type element to the Schedule to identify a long or short spread value.
    spreadScheduleType: SpreadScheduleTypeEnum (optional)

type StubFloatingRate  # A class defining a floating rate.
    floatingRateIndex: FloatingRateIndexEnum (required)
    indexTenor: Period (optional)
    floatingRateMultiplierSchedule: Schedule (optional)
    spreadSchedule: SpreadSchedule (list)
    rateTreatment: RateTreatmentEnum (optional)
    capRateSchedule: StrikeSchedule (list)
    floorRateSchedule: StrikeSchedule (list)

type StubValue  # A type defining how a stub calculation period amount is calculated. A single floating rate tenor different to that used 
    floatingRate: StubFloatingRate (0..2)
    stubRate: number (optional)
    stubAmount: Money (optional)

type FixedAmountCalculationDetails  # Type for reporting the detailed results of calculating a cash flow for a calculation period.  This is enhanced relative 
    calculationPeriod: CalculationPeriodBase (required)
    calculationPeriodNotionalAmount: Money (required)
    fixedRate: number (required)
    yearFraction: number (required)
    calculatedAmount: number (required)

type FloatingAmountCalculationDetails  # Type for reporting the detailed results of calculating a cash flow for a calculation period.  This is enhanced relative 
    calculationPeriod: CalculationPeriodBase (required)
    calculationPeriodNotionalAmount: Money (required)
    floatingRate: FloatingRateSettingDetails (optional)
    processingDetails: FloatingRateProcessingDetails (optional)
    appliedRate: number (required)
    yearFraction: number (required)
    calculatedAmount: number (required)
    spreadExclusiveCalculatedAMount: number (required)

type FloatingRateSettingDetails  # Type for reporting the raw (untreated) observed or calculated rate for a calculation period.  If this is a calculated ra
    calculationDetails: CalculatedRateDetails (optional)
    observationDate: date (optional)
    resetDate: date (optional)
    floatingRate: number (required)

type FloatingRateProcessingDetails  # Type for reporting the details of the rate treatment.  This could potentially be replaced by the existing FloatingRateDe
    rawRate: number (required)
    processingParameters: FloatingRateProcessingParameters (optional)
    processedRate: number (required)
    spreadExclusiveRate: number (required)

type FloatingRateProcessingParameters  # Type to hold the processing parameters that should be or were used to calculate a floating amount.  These parameters can
    initialRate: Price (optional)
    multiplier: number (optional)
    spread: number (optional)
    treatment: RateTreatmentEnum (optional)
    capRate: number (optional)
    floorRate: number (optional)
    rounding: Rounding (optional)
    negativeTreatment: NegativeInterestRateTreatmentEnum (optional)

type ComputedAmount  # A class to specify the outcome of a computed amount, for testing purposes.
    callFunction: calculation (required)
    amount: number (required)
    currency: string (optional)

type AssetFlowBase  # Defines the basic parameters of an asset transfer, e.g. a cashflow: what (the asset), how much (the quantity) and when (
    quantity: NonNegativeQuantity (required)
    asset: Asset (required)
    settlementDate: AdjustableOrAdjustedOrRelativeDate (required)

type Cashflow extends AssetFlowBase  # Class to specify a cashflow, i.e. the outcome of either of computation (e.g. interest accrual) or an assessment of some 
    payerReceiver: PayerReceiver (required)
    cashflowType: CashflowType (required)
    paymentDiscounting: PaymentDiscounting (optional)

type CashflowType  # Characterises the type of cashflow, which can result from either a scheduled or a non-scheduled lifecycle event.
    cashflowType: ScheduledTransferEnum (optional)
    cashPrice: CashPrice (optional)
    priceExpression: PriceExpressionEnum (optional)

type QuantityMultiplier  # Class to specify a mechanism for a quantity to be set as a multiplier to another (reference) quantity, based on a price 
    fxLinkedNotionalSchedule: FxLinkedNotionalSchedule (optional)
    multiplierValue: number (optional)

type ResolvablePriceQuantity  # Generic class to specify the quantity for different payout legs in a contractual product, when that quantity can vary ac
    resolvedQuantity: Quantity (optional)
    quantitySchedule: NonNegativeQuantitySchedule (optional)
    quantityReference: ResolvablePriceQuantity (optional)
    quantityMultiplier: QuantityMultiplier (optional)
    reset: boolean (optional)
    futureValueNotional: FutureValueAmount (optional)
    priceSchedule: PriceSchedule (list)

type PayoutBase  # A data type that contains the common attributes (e.g. payer and receiver parties) and validation conditions that apply a
    payerReceiver: PayerReceiver (required)
    priceQuantity: ResolvablePriceQuantity (optional)
    principalPayment: PrincipalPayments (optional)
    settlementTerms: SettlementTerms (optional)

type CommodityPriceReturnTerms  # Defines parameters in which the commodity price is assessed.
    rounding: Rounding (optional)
    spread: SpreadSchedule (optional)
    rollFeature: RollFeature (optional)
    conversionFactor: number (optional)

type RollFeature  # Used in conjunction with an exchange-based pricing source. Identifies a way in which the futures contracts referenced wi
    rollSourceCalendar: RollSourceCalendarEnum (optional)
    deliveryDateRollConvention: Offset (optional)

type PricingDates  # Specifies specific dates or parametric rules for the dates on which the price will be determined
    specifiedDates: AdjustableDates (list)
    parametricDates: ParametricDates (optional)

type ValuationDate  # A single object that represents the different methods to specify a valuation date, as used for cash settlement. The Sing
    singleValuationDate: SingleValuationDate (optional)
    multipleValuationDates: MultipleValuationDates (optional)
    valuationDate: RelativeDateOffset (optional)
    fxFixingDate: FxFixingDate (optional)
    fxFixingSchedule: AdjustableDates (optional)

type CashSettlementTerms  # Defines the terms required to compute and settle a cash settlement amount according to a fixing value, including the fix
    cashSettlementMethod: CashSettlementMethodEnum (optional)
    valuationMethod: ValuationMethod (optional)
    valuationDate: ValuationDate (optional)
    valuationTime: BusinessCenterTime (optional)
    cashSettlementAmount: Money (optional)
    recoveryFactor: number (optional)
    fixedSettlement: boolean (optional)
    accruedInterest: boolean (optional)

type DeliverableObligations  # A class to specify all the ISDA terms relevant to defining the deliverable obligations.
    accruedInterest: boolean (optional)
    category: ObligationCategoryEnum (optional)
    notSubordinated: boolean (optional)
    specifiedCurrency: SpecifiedCurrency (optional)
    notSovereignLender: boolean (optional)
    notDomesticCurrency: NotDomesticCurrency (optional)
    notDomesticLaw: boolean (optional)
    listed: boolean (optional)
    notContingent: boolean (optional)
    notDomesticIssuance: boolean (optional)
    assignableLoan: PCDeliverableObligationCharac (optional)
    consentRequiredLoan: PCDeliverableObligationCharac (optional)
    directLoanParticipation: LoanParticipation (optional)
    transferable: boolean (optional)
    maximumMaturity: Period (optional)
    acceleratedOrMatured: boolean (optional)
    notBearer: boolean (optional)
    fullFaithAndCreditObLiability: boolean (optional)
    generalFundObligationLiability: boolean (optional)
    revenueObligationLiability: boolean (optional)
    indirectLoanParticipation: LoanParticipation (optional)
    excluded: string (optional)
    othReferenceEntityObligations: string (optional)

type LoanParticipation extends PCDeliverableObligationCharac  # A class to specify loan with a participation agreement whereby the buyer is capable of creating, or procuring the creati
    qualifyingParticipationSeller: string (optional)

type PCDeliverableObligationCharac  # A class to specify the Partial Cash Deliverable Obligation Characteristic.
    applicable: boolean (required)
    partialCashSettlement: boolean (optional)

type PhysicalSettlementPeriod
    businessDaysNotSpecified: boolean (optional)
    businessDays: int (optional)
    maximumBusinessDays: int (optional)

type PhysicalSettlementTerms  # Specifies Physical Settlement Terms characteristics for the settlement of a Credit Default Swap or Option.
    clearedPhysicalSettlement: boolean (optional)
    predeterminedClearingOrganizationParty: AncillaryRoleEnum (optional)
    physicalSettlementPeriod: PhysicalSettlementPeriod (optional)
    deliverableObligations: DeliverableObligations (optional)
    escrow: boolean (optional)
    sixtyBusinessDaySettlementCap: boolean (optional)

type PercentageRule  # A class defining a content model for a calculation rule defined as percentage of the notional amount.
    paymentPercent: number (required)
    notionalAmountReference: Money (required)

type SettlementDate  # A data defining the settlement date(s) for cash or physical settlement as either a set of explicit dates, together with 
    adjustableOrRelativeDate: AdjustableOrAdjustedOrRelativeDate (optional)
    valueDate: date (optional)
    adjustableDates: AdjustableDates (optional)
    businessDateRange: BusinessDateRange (optional)
    cashSettlementBusinessDays: int (optional)
    paymentDelay: boolean (optional)

type FxFixingDate extends Offset  # Extends the Offset structure to specify an FX fixing date as an offset to dates specified somewhere else in the document
    businessDayConvention: BusinessDayConventionEnum (optional)
    businessCenters: BusinessCenters (optional)
    businessCentersReference: BusinessCenters (optional)
    dateRelativeToPaymentDates: DateRelativeToPaymentDates (optional)
    dateRelativeToCalculationPeriodDates: DateRelativeToCalculationPeriodDates (optional)
    dateRelativeToValuationDates: DateRelativeToValuationDates (optional)
    fxFixingDate: AdjustableOrRelativeDate (optional)

type SettlementTerms extends SettlementBase  # Specifies the settlement terms, which can either be cash, physical, or fx-based cash-settlement. This class can be used 
    cashSettlementTerms: CashSettlementTerms (list)
    physicalSettlementTerms: PhysicalSettlementTerms (optional)

type FixedPrice  # A predefined price accorded by the counterparties.
    price: PriceSchedule (optional)

type PaymentDetail
    paymentDate: AdjustableOrRelativeDate (optional)
    paymentRule: PaymentRule (required)
    paymentAmount: Money (optional)

type PaymentDiscounting  # This class corresponds to the FpML PaymentDiscounting.model group for representing the discounting elements that can be 
    discountFactor: number (optional)
    presentValueAmount: Money (optional)

type PaymentRule  # A class defining the payment calculation rule. As of FpML 5.10, percentage rule is the only calculation rule that has be
    percentageRule: PercentageRule (optional)

type PrincipalPayments  # A class defining which principal exchanges occur for the stream.
    initialPayment: boolean (required)
    finalPayment: boolean (required)
    intermediatePayment: boolean (required)
    varyingLegNotionalCurrency: string (list)
    principalPaymentSchedule: PrincipalPaymentSchedule (optional)

type PrincipalPaymentSchedule  # Describe dates schedules for Principal Exchanges and related role of the parties when known.
    initialPrincipalPayment: PrincipalPayment (optional)
    intermediatePrincipalPayment: AdjustableRelativeOrPeriodicDates (optional)
    finalPrincipalPayment: PrincipalPayment (optional)

type PrincipalPayment  # Any kind of principal payments when the amount is known and thus fixed.
    principalPaymentDate: AdjustableDate (optional)
    payerReceiver: PayerReceiver (optional)
    principalAmount: Money (optional)
    discountFactor: number (optional)
    presentValuePrincipalAmount: Money (optional)

type SettlementBase  # A base class to be extended by the SettlementTerms class.
    settlementType: SettlementTypeEnum (required)
    transferSettlementType: TransferSettlementEnum (optional)
    settlementCurrency: string (optional)
    settlementDate: SettlementDate (optional)
    settlementCentre: SettlementCentreEnum (optional)
    settlementProvision: SettlementProvision (optional)
    standardSettlementStyle: StandardSettlementStyleEnum (optional)

type SettlementProvision  # Defines parameters that regulate a settlement, for instance whether this settlement should be netted with other ones or 
    shapingProvisions: ShapingProvision (optional)

type ShapingProvision  # Defines the applicable settlement limits that may require a settlement to be 'shaped', i.e. broken-down into smaller amo
    shapeSchedule: Money (list)

type Counterparty  # Defines a counterparty enumerated value, e.g. Party1 or Party2, with an associated party reference. The product is agnos
    role: CounterpartyRoleEnum (required)
    partyReference: Party (required)

type AncillaryParty  # Defines an ancillary role enumerated value with an associated party reference. The product is agnostic to the actual par
    role: AncillaryRoleEnum (required)
    partyReference: Party (list)
    onBehalfOf: CounterpartyRoleEnum (optional)

type BuyerSeller  # This class corresponds to the FpML BuyerSeller.model construct.
    buyer: CounterpartyRoleEnum (required)
    seller: CounterpartyRoleEnum (required)

type PayerReceiver  # Specifies the parties responsible for making and receiving payments defined by this structure.
    payer: CounterpartyRoleEnum (required)
    receiver: CounterpartyRoleEnum (required)

type PartyReferencePayerReceiver  # Specifies the parties responsible for making and receiving payments defined by this structure.
    payerPartyReference: Party (required)
    payerAccountReference: Account (optional)
    receiverPartyReference: Party (required)
    receiverAccountReference: Account (optional)

type ReferenceBank  # A class to describe an institution (party) identified by means of a coding scheme and an optional name.
    referenceBankId: string (required)
    referenceBankName: string (optional)

type ReferenceBanks  # A class defining the list of reference institutions polled for relevant rates or prices when determining the cash settle
    referenceBank: ReferenceBank (list)

type RelatedParty
    partyReference: Party (required)
    accountReference: Account (optional)
    role: PartyRoleEnum (required)

type Account  # A class to specify an account as an account number alongside, optionally. an account name, an account type, an account b
    partyReference: Party (optional)
    accountNumber: string (required)
    accountName: string (optional)
    accountType: AccountTypeEnum (optional)
    accountBeneficiary: Party (optional)
    servicingParty: Party (optional)

type Address  # A class to specify a post or street address.
    street: string (list)
    city: string (optional)
    state: string (optional)
    country: string (optional)
    postalCode: string (optional)

type BusinessUnit  # A class to specify an organizational unit.
    name: string (required)
    identifier: Identifier (optional)
    contactInformation: ContactInformation (optional)

type ContactInformation  # A class to specify contact information associated with a party: telephone, postal/street address, email and web page.
    telephone: TelephoneNumber (list)
    address: Address (list)
    email: string (list)
    webPage: string (list)

type LegalEntity  # A class to specify a legal entity, with a required name and an optional entity identifier (such as the LEI).
    entityId: string (list)
    name: string (required)
    entityIdentifier: EntityIdentifier (list)

type NaturalPerson  # A class to represent the attributes that are specific to a natural person.
    personId: PersonIdentifier (list)
    honorific: string (optional)
    firstName: string (optional)
    middleName: string (list)
    initial: string (list)
    surname: string (optional)
    suffix: string (optional)
    dateOfBirth: date (optional)
    contactInformation: ContactInformation (optional)

type NaturalPersonRole  # A class to specify the role(s) that natural person(s) may have in relation to the contract.
    personReference: NaturalPerson (required)
    role: NaturalPersonRoleEnum (list)

type Party  # A class to specify a party, without a qualification as to whether this party is a legal entity or a natural person, alth
    partyId: PartyIdentifier (list)
    name: string (optional)
    businessUnit: BusinessUnit (list)
    person: NaturalPerson (list)
    personRole: NaturalPersonRole (list)
    account: Account (optional)
    contactInformation: ContactInformation (optional)

type PartyIdentifier  # Comprises an identifier and a source. The associated metadata key denotes the ability to associate a hash value to the P
    identifier: string (required)
    identifierType: PartyIdentifierTypeEnum (optional)

type EntityIdentifier  # Comprises an identifier and a source. The associated metadata key denotes the ability to associate a hash value to the E
    identifier: string (required)
    identifierType: EntityIdentifierTypeEnum (optional)

type PersonIdentifier  # Comprises an identifier and a source. The associated metadata key denotes the ability to associate a hash value to the P
    identifier: string (required)
    identifierType: PersonIdentifierTypeEnum (optional)
    country: string (optional)

type PartyContactInformation  # A class to specify contact information within a party: address and, optionally, associated business unit and person. Thi
    partyReference: Party (optional)
    contactInformation: ContactInformation (optional)
    businessUnit: BusinessUnit (list)
    person: NaturalPerson (list)
    additionalInformation: string (optional)

type PartyRole  # A class to specify the role(s) that party(ies) may have in relation to the execution, contract or other legal agreement.
    partyReference: Party (required)
    role: PartyRoleEnum (required)
    ownershipPartyReference: Party (optional)

type TelephoneNumber  # A class to specify a telephone number as a type of phone number (e.g. work, personal, ...) alongside with the actual num
    telephoneNumberType: TelephoneTypeEnum (optional)
    number: string (required)

type AncillaryEntity  # Holds an identifier for an ancillary entity, either identified directly via its ancillary role or directly as a legal en
    ancillaryParty: AncillaryRoleEnum (optional)
    legalEntity: LegalEntity (optional)

type AdjustableDate  # A class for defining a date that shall be subject to adjustment if it would otherwise fall on a day that is not a busine
    unadjustedDate: date (optional)
    dateAdjustments: BusinessDayAdjustments (optional)
    dateAdjustmentsReference: BusinessDayAdjustments (optional)
    adjustedDate: date (optional)

type AdjustableDates  # A class for defining a series of dates that shall be subject to adjustment if they would otherwise fall on a day that is
    unadjustedDate: date (list)
    dateAdjustments: BusinessDayAdjustments (optional)
    adjustedDate: date (list)

type AdjustableOrAdjustedDate  # A class for defining a date that shall be subject to adjustment if it would otherwise fall on a day that is not a busine
    unadjustedDate: date (optional)
    dateAdjustments: BusinessDayAdjustments (optional)
    adjustedDate: date (optional)

type AdjustableOrAdjustedOrRelativeDate  # This Rosetta class specifies the date as either an unadjusted, adjusted or relative date. It supplements the features of
    unadjustedDate: date (optional)
    dateAdjustments: BusinessDayAdjustments (optional)
    adjustedDate: date (optional)
    relativeDate: RelativeDateOffset (optional)

type AdjustableOrRelativeDate  # A class giving the choice between defining a date as an explicit date together with applicable adjustments or as relativ
    adjustableDate: AdjustableDate (optional)
    relativeDate: AdjustedRelativeDateOffset (optional)

type AdjustableOrRelativeDates  # A class giving the choice between defining a series of dates as an explicit list of dates together with applicable adjus
    adjustableDates: AdjustableDates (optional)
    relativeDates: RelativeDates (optional)

type AdjustedRelativeDateOffset extends RelativeDateOffset  # A type defining a date (referred to as the derived date) as a relative offset from another date (referred to as the anch
    relativeDateAdjustments: BusinessDayAdjustments (optional)

type BusinessCenters  # A class for specifying the business day calendar location used in determining whether a day is a business day or not, ei
    businessCenter: BusinessCenterEnum (list)
    commodityBusinessCalendar: CommodityBusinessCalendarEnum (list)
    businessCentersReference: BusinessCenters (optional)

type BusinessCenterTime  # A class for defining a time with respect to a business day calendar location. For example, 11:00:00 GBLO.
    hourMinuteTime: time (required)
    businessCenter: BusinessCenterEnum (required)

type BusinessDateRange extends DateRange  # A class defining a range of contiguous business days by defining an unadjusted first date, an unadjusted last date and a
    businessDayConvention: BusinessDayConventionEnum (required)
    businessCenters: BusinessCenters (optional)

type BusinessDayAdjustments  # A class defining the business day convention and financial business centers used for adjusting any relevant date if it w
    businessDayConvention: BusinessDayConventionEnum (required)
    businessCenters: BusinessCenters (optional)

type DateRange  # A class defining a contiguous series of calendar dates. The date range is defined as all the dates between and including
    startDate: date (required)
    endDate: date (required)

type DateList  # List of dates.
    date: date (list)

type DateTimeList  # List of dateTimes.
    dateTime: zonedDateTime (list)

type Frequency  # A class for defining a date frequency, e.g. one day, three months, through the combination of an integer value and a sta
    periodMultiplier: int (required)
    period: PeriodExtendedEnum (required)

type Offset extends Period  # A class defining an offset used in calculating a new date relative to a reference date, e.g. calendar days, business day
    dayType: DayTypeEnum (optional)

type CustomisableOffset  # A class to specify an offset either as a normalized [multiplier, period, dayType] or as a custom provision of type strin
    offset: Offset (optional)
    customProvision: string (optional)

type Period  # A class to define recurring periods or time offsets.
    periodMultiplier: int (required)
    period: PeriodEnum (required)

type RelativeDates extends RelativeDateOffset  # A class describing a set of dates defined as relative to another set of dates.
    periodSkip: int (optional)
    scheduleBounds: DateRange (optional)

type RelativeDateOffset extends Offset  # A class defining a date (referred to as the derived date) as a relative offset from another date (referred to as the anc
    businessDayConvention: BusinessDayConventionEnum (required)
    businessCenters: BusinessCenters (optional)
    businessCentersReference: BusinessCenters (optional)
    dateRelativeTo: date (optional)
    adjustedDate: date (optional)

type TimeZone  # The time alongside with the timezone location information. This class makes use of the FpML TimezoneLocation construct.
    time: time (required)
    location: string (optional)

type PeriodRange  # Indicates The period range defined as either a lower and upper period bound, or both.
    lowerBound: PeriodBound (optional)
    upperBound: PeriodBound (optional)

type PeriodBound  # Indicator to specify if the period bound is defined as a period and whether the bound is inclusive.
    period: Period (required)
    inclusive: boolean (required)

type AveragingSchedule  # Class to representing a method for generating a series of dates.
    startDate: date (required)
    endDate: date (required)
    averagingPeriodFrequency: CalculationPeriodFrequency (required)

type AdjustableRelativeOrPeriodicDates  # A class giving the choice between defining a series of dates as an explicit list of dates together with applicable adjus
    adjustableDates: AdjustableDates (optional)
    relativeDates: RelativeDates (optional)
    periodicDates: PeriodicDates (optional)

type PeriodicDates  # A class for specifying a calculation period schedule.
    startDate: AdjustableOrRelativeDate (optional)
    endDate: AdjustableOrRelativeDate (optional)
    periodFrequency: CalculationPeriodFrequency (optional)
    periodDatesAdjustments: BusinessDayAdjustments (optional)
    dayType: DayTypeEnum (optional)

type CalculationPeriodFrequency extends Frequency  # A class to specify the frequency at which calculation period end dates occur within the regular part of the calculation 
    rollConvention: RollConventionEnum (required)
    balanceOfFirstPeriod: boolean (optional)

type CalculationFrequency  # Represents the parameters for describing how often something (such as collateral interest) is to be calculated.
    period: Period (required)
    monthOfYear: number (optional)
    dayOfMonth: number (optional)
    dayOfWeek: DayOfWeekEnum (optional)
    weekOfMonth: number (optional)
    offsetDays: number (required)
    dateLocation: BusinessCenterTime (required)
    businessCenter: BusinessCenterEnum (list)

type IndexBase extends AssetBase  # Identifies an index by referencing an identifier.
    name: string (optional)
    provider: LegalEntity (optional)
    assetClass: AssetClassEnum (optional)

type FloatingRateIndex extends IndexBase  # Specification of an interest rate index which can change over time, e.g. the SONIA (Sterling Overnight Index Average) in
    floatingRateIndex: FloatingRateIndexEnum (required)
    indexTenor: Period (optional)

type ForeignExchangeRateIndex extends IndexBase  # Specification of a rate based on the exchange of a pair of cash assets in specific currencies, e.g. USD versus GBP.
    quotedCurrencyPair: QuotedCurrencyPair (required)
    primaryFxSpotRateSource: InformationSource (required)
    secondaryFxSpotRateSource: InformationSource (optional)

type InflationIndex extends IndexBase  # Specification of an index that measures inflation in a specific market, e.g. the US Consumer Price Index.
    inflationRateIndex: InflationRateIndexEnum (required)
    indexTenor: Period (optional)

type CreditIndex extends IndexBase  # Specification of an index based on credit risk, typically composed using corporate debt instruments in a region or indus
    indexSeries: int (optional)
    indexAnnexVersion: int (optional)
    indexAnnexDate: date (optional)
    indexAnnexSource: IndexAnnexSourceEnum (optional)
    excludedReferenceEntity: ReferenceInformation (list)
    tranche: Tranche (optional)
    settledEntityMatrix: SettledEntityMatrix (optional)
    indexFactor: number (optional)
    seniority: CreditSeniorityEnum (optional)

type EquityIndex extends IndexBase  # Specification of an index based on equity securities, e.g. the S&P 500..

type OtherIndex extends IndexBase  # Specification of a user-defined index that does not meet the criteria of other Index data types.
    description: string (optional)

type PriceQuantity  # Defines a settlement as an exchange between two parties of a specified quantity of an asset (the quantity) against a spe
    price: PriceSchedule (list)
    quantity: NonNegativeQuantitySchedule (list)
    observable: Observable (optional)
    effectiveDate: AdjustableOrRelativeDate (optional)

type PremiumExpression  # This class corresponds to the FpML Premium.model group for representing the option premium when expressed in a way other
    premiumType: PremiumTypeEnum (optional)
    pricePerOption: Money (optional)
    percentageOfNotional: number (optional)

type CashPrice  # Specifies the nature of a cash price either as a fee type, cash price type, or premium expression.
    cashPriceType: CashPriceTypeEnum (required)
    premiumExpression: PremiumExpression (optional)
    feeType: FeeTypeEnum (optional)

type PriceComposite  # Defines the inputs required to calculate a price as a simple composite of 2 other values. The inputs consist of 2 number
    baseValue: number (required)
    operand: number (required)
    arithmeticOperator: ArithmeticOperationEnum (required)
    operandType: PriceOperandEnum (optional)

type PriceSchedule extends MeasureSchedule  # Specifies the price of a financial instrument in a trade as a schedule of measures. A price generically expresses the va
    perUnitOf: UnitType (optional)
    priceType: PriceTypeEnum (required)
    priceExpression: PriceExpressionEnum (optional)
    composite: PriceComposite (optional)
    arithmeticOperator: ArithmeticOperationEnum (optional)
    cashPrice: CashPrice (optional)

type Price extends PriceSchedule  # Specifies a price as a single value to be associated to a financial product. This data type extends PriceSchedule and re

type Basket extends AssetBase  # Defines a custom basket by referencing an identifier and its constituents.
    basketConstituent: BasketConstituent (list)

type BasketConstituent extends Observable  # Identifies the constituents of the basket
    quantity: NonNegativeQuantitySchedule (list)
    initialValuationPrice: PriceSchedule (list)
    interimValuationPrice: PriceSchedule (list)
    finalValuationPrice: PriceSchedule (list)

type InformationSource  # A class defining the source for a piece of information (e.g. a rate fix or an FX fixing). The attribute names have been 
    sourceProvider: InformationProviderEnum (required)
    sourcePage: string (optional)
    sourcePageHeading: string (optional)

type Money extends Quantity  # Defines a monetary amount in a specified currency.

type QuotedCurrencyPair  # A class that describes the composition of a rate that has been quoted or is to be quoted. This includes the two currenci
    currency1: string (required)
    currency2: string (required)
    quoteBasis: QuoteBasisEnum (required)

type Curve
    interestRateCurve: InterestRateCurve (optional)
    commodityCurve: CommodityReferencePriceEnum (optional)

type InterestRateCurve
    floatingRateIndex: FloatingRateIndexEnum (required)
    tenor: Period (required)

type TransactedPrice  # A class to represent the transacted price attributes that are positioned as part of the FpML FeeLeg.
    marketFixedRate: number (optional)
    initialPoints: number (optional)
    marketPrice: number (optional)
    quotationStyle: QuotationStyleEnum (optional)

type ReferenceSwapCurve  # A complex type used to specify the option and convertible bond option strike when expressed in reference to a swap curve
    swapUnwindValue: SwapCurveValuation (required)
    makeWholeAmount: MakeWholeAmount (optional)

type MakeWholeAmount extends SwapCurveValuation  # A class to specify the amount to be paid by the buyer of the option if the option is exercised prior to the Early Call D
    interpolationMethod: InterpolationMethodEnum (optional)
    earlyCallDate: date (required)

type SwapCurveValuation  # A class to specify a valuation swap curve, which is used as part of the strike construct for the bond and convertible bo
    floatingRateIndex: FloatingRateIndexEnum (required)
    indexTenor: Period (optional)
    spread: number (required)
    side: QuotationSideEnum (optional)

type ValuationDates  # Defines how and when a performance type option or performance type swap is to be valued, including initial, interim and 
    initialValuationDate: PerformanceValuationDates (optional)
    interimValuationDate: PerformanceValuationDates (optional)
    finalValuationDate: PerformanceValuationDates (required)

type PerformanceValuationDates  # Defines how and when a performance type option or performance type swap is to be valued.
    determinationMethod: DeterminationMethodEnum (required)
    valuationDates: AdjustableRelativeOrPeriodicDates (optional)
    valuationDate: AdjustableOrRelativeDate (optional)
    valuationTime: BusinessCenterTime (optional)
    valuationTimeType: TimeTypeEnum (optional)

type FxRateSourceFixing  # Describes a rate source to be fixed and the date the fixing occurs
    settlementRateSource: FxSettlementRateSource (required)
    fixingDate: AdjustableDate (required)

type FxInformationSource extends InformationSource  # Information source specific to Foreign Exchange products.
    fixingTime: BusinessCenterTime (optional)

type FallbackReferencePrice  # The method, prioritised by the order it is listed in this element, to get a replacement rate for the disrupted settlemen
    valuationPostponement: ValuationPostponement (optional)
    fallBackSettlementRateOption: SettlementRateOptionEnum (list)
    fallbackSurveyValuationPostponement: boolean (optional)
    calculationAgentDetermination: CalculationAgent (optional)

type ValuationPostponement  # Specifies how long to wait to get a quote from a settlement rate option upon a price source disruption.
    maximumDaysOfPostponement: int (required)

type FxRate  # A class describing the rate of a currency conversion: pair of currency, quotation mode and exchange rate.
    quotedCurrencyPair: QuotedCurrencyPair (required)
    rate: number (optional)

type FxSpotRateSource  # A class defining the rate source and fixing time for an FX rate.
    primarySource: InformationSource (required)
    secondarySource: InformationSource (optional)

type RateObservation  # A class defining parameters associated with an individual observation or fixing. This class forms part of the cashflow r
    resetDate: date (optional)
    adjustedFixingDate: date (optional)
    observedRate: number (optional)
    treatedRate: number (optional)
    observationWeight: int (optional)
    rateReference: RateObservation (optional)
    forecastRate: number (optional)
    treatedForecastRate: number (optional)

type CreditNotation  # Represents a class to specify the credit notation as the combination of agency, notation, scale and debt type qualificat
    agency: CreditRatingAgencyEnum (required)
    notation: string (required)
    scale: string (optional)
    debt: CreditRatingDebt (optional)
    outlook: CreditRatingOutlookEnum (optional)
    creditWatch: CreditRatingCreditWatchEnum (optional)

type CreditNotations  # Represents the credit rating notation higher level construct, which provides the ability to specify multiple rating nota
    creditNotation: CreditNotation (optional)
    creditNotations: MultipleCreditNotations (optional)

type CreditRatingDebt  # Specifies the credit rating debt type(s) associated with the credit rating notation and scale. When several debt types a
    debtType: string (optional)
    debtTypes: MultipleDebtTypes (optional)

type MultipleCreditNotations  # Represetns a class to specify multiple credit notations alongside a conditional 'any' or 'all' qualifier.
    condition: QuantifierEnum (required)
    creditNotation: CreditNotation (2..*)
    mismatchResolution: CreditNotationMismatchResolutionEnum (optional)
    referenceAgency: CreditRatingAgencyEnum (optional)

type MultipleDebtTypes  # Represents a class to specify multiple credit debt types alongside a conditional 'any' or 'all' qualifier.
    condition: QuantifierEnum (required)
    debtType: string (2..*)

type FxSettlementRateSource  # The source of the Foreign Exchange settlement rate.
    settlementRateOption: string (optional)
    nonstandardSettlementRate: FxInformationSource (optional)

type CalculationAgent  # A class defining the ISDA calculation agent responsible for performing duties as defined in the applicable product defin
    calculationAgentParty: AncillaryRoleEnum (optional)
    calculationAgentPartyEnum: PartyDeterminationEnum (optional)
    calculationAgentBusinessCenter: BusinessCenterEnum (optional)

type SettlementRateOption  # Defines the settlement rate option to use for fixing in case of cash settlement. Currently only applicable to foreign ex
    settlementRateOption: SettlementRateOptionEnum (required)
    priceSourceDisruption: PriceSourceDisruption (optional)

type MultipleValuationDates extends SingleValuationDate
    businessDaysThereafter: int (optional)
    numberValuationDates: int (optional)

type SingleValuationDate  # A class to specify the number of business days after satisfaction of all conditions to settlement.
    businessDays: int (optional)

type ValuationSource  # A class describing the method for obtaining a settlement rate, specified through either an information source (page), a 
    quotedCurrencyPair: QuotedCurrencyPair (optional)
    informationSource: FxSpotRateSource (optional)
    settlementRateOption: SettlementRateOption (optional)
    referenceBanks: ReferenceBanks (optional)
    dealerOrCCP: AncillaryEntity (optional)

type PriceSourceDisruption  # A data defining:  the parameters used to get a price quote to replace the settlement rate option that is disrupted.
    fallbackReferencePrice: FallbackReferencePrice (required)

type ValuationMethod  # Specifies the parameters required to obtain a valuation, including the source, quotation method (bid, mid etc.) and any 
    valuationSource: ValuationSource (required)
    quotationMethod: QuotationRateTypeEnum (optional)
    valuationMethod: ValuationMethodEnum (optional)
    quotationAmount: Money (optional)
    minimumQuotationAmount: Money (optional)
    cashCollateralValuationMethod: CashCollateralValuationMethod (optional)

type CashCollateralValuationMethod  # This type is a generic structure that can represent the parameters of several mid-market valuation and replacement value
    applicableCsa: CsaTypeEnum (optional)
    cashCollateralCurrency: string (optional)
    cashCollateralInterestRate: string (optional)
    agreedDiscountRate: string (optional)
    protectedParty: PartyDeterminationEnum (0..2)
    prescribedDocumentationAdjustment: boolean (optional)

type DividendApplicability  # The parameters which define whether dividends are applicable
    optionsExchangeDividends: boolean (optional)
    additionalDividends: boolean (optional)
    allDividends: boolean (optional)

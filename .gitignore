.env
.venv
__pycache__/
*.pyc

# Maven build output
**/target/

data/cdm-fpml_pairs/
runs/
.llm_io/
.mcp_pids/
.mcp_logs/

# Generated Maven projects + run logs produced by the agent on each run
# (regenerated every run, ~GBs of jars/classes — never commit; findings live in the reports)
workspaces/
# stray run logs at repo root
*.launch.log

# Claude Code local state
.claude/
